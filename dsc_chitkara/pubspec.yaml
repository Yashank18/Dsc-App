name: dsc_chitkara
description: A new Flutter project.
version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.2
  font_awesome_flutter:
  url_launcher: ^5.4.2
  cached_network_image: ^2.0.0
  google_fonts: ^0.3.10
  share: ^0.6.4
  image_picker_saver: ^0.3.0
  path_provider: ^1.6.5
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: 
    

flutter_icons:
  
  android: true
  ios: true
  image_path: "images/llogo.jpg" 

flutter:
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
   - images/
